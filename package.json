{
  "name": "server-from-scratch",
  "type": "module",
  "module": "index.ts",
  "scripts": {
    "start:echo": "cd echo-server && npx tsx --enable-source-maps socket.ts",
    "start:newline-proto": "cd protocols && npx tsx --enable-source-maps newline-proto.ts",
    "start:http": "cd protocols && npx tsx --enable-source-maps http-1.ts"
  },
  "overrides": {
    "whatwg-url": "13.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "tsx": "latest"
  }
}
